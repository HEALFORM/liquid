import tokens from '@healform/design-tokens/dist/js/tokens'

export const SubHeadline = {
  baseStyle: {
    textTransform: 'uppercase',
    fontWeight: tokens.fonts.fontWeight.bold.value,
    fontSize: tokens.typography.subHeadline.fontSize.value,
    lineHeight: tokens.typography.subHeadline.lineHeight.value,
    fontFamily: tokens.fonts.fontStack.special.value,
  },
}
